# 原理：每次从未排序的数组中取一个数，和已排序的数组中的数进行比较，然后放入到对应的位置。
# 伪代码：
Insertion-sort(A):
  for j = 2 to A.length: # 直接从第二个数开始排序，因为第一个数不用排序
    key = A[j]           # 取出需要排序的数
    i = j-1              # 排序数组的下标
    while i>0 and A[i]>key：# 在while中直接做出数值比较判断。
      A[i+1]=A[i]        # 修改其他元素的下标位置，即移动元素位置
      i=i-1              
    A[i+1]=key           # 将对应的元素插入位置
# 题目：
